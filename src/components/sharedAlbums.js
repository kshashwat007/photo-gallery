import React from 'react';
import { Text, View } from 'react-native';

const SharedAlbums = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Shared Albums Page</Text>
    </View>
  );
};

export default SharedAlbums;
